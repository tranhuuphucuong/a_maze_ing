require "spec_helper"

RSpec.describe AMazeIng do
  it "has a version number" do
    expect(AMazeIng::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
